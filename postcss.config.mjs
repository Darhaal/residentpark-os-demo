// Title: PostCSS Configuration
// Path: postcss.config.mjs
// Functionality: PostCSS plugin configuration used by the application styling pipeline.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
