// Title: Resident Parking Loading
// Path: src/app/parking/loading.tsx
// Functionality: Route-level skeleton for the resident parking map while server data streams.

import { ResidentPortalLoading } from '@/components/resident/ResidentPortalLoading';

export default function ParkingLoading() {
  return <ResidentPortalLoading variant="parking" />;
}
