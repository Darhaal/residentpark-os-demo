// Title: Profile Loading
// Path: src/app/profile/loading.tsx
// Functionality: Route-level skeleton for the resident profile while server data streams.

import { ResidentPortalLoading } from '@/components/resident/ResidentPortalLoading';

export default function ProfileLoading() {
  return <ResidentPortalLoading variant="profile" />;
}
